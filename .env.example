# Política de reinício para os containers (ex: no, on-failure, unless-stopped, always)
RESTART_POLICY=unless-stopped

# --- Configurações do PostgreSQL ---
POSTGRES_DB=mainmotor
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# --- Limites de Recursos do Banco de Dados ---
DB_MEMORY_LIMIT=1g
DB_CPU_LIMIT=1.0

# --- Configurações da API ---
# Ambiente do ASP.NET Core (ex: Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Development
# Nível de log (ex: Trace, Debug, Information, Warning, Error, Critical, None)
LOG_LEVEL=Information
API_PORT=8080

# --- Limites de Recursos da API ---
API_MEMORY_LIMIT=512m
API_CPU_LIMIT=1.0

# --- Configuração da Rede Docker ---
# Garanta que esta sub-rede não conflite com outras redes locais
DOCKER_SUBNET=172.20.0.0/16